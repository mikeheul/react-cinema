import React from 'react';
import Header from '../components/Header';

const UserList = () => {
    return (
        <div>
            <Header />
            <h1>User List</h1>
        </div>
    );
};

export default UserList;